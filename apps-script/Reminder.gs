/**
 * Reminder.gs — (skeleton)
 *
 * Stock App ใช้ Alert.gs สำหรับ daily alert (08:00) — checkLowStock + checkExpiringLots
 * ไฟล์นี้เก็บไว้เผื่ออนาคต (เช่น reminder ถ้า lot ใกล้หมด > 3 วันไม่มีคนเช็ค)
 *
 * ดู Alert.gs สำหรับ daily trigger ของ Stock App
 */

// (intentionally empty — เก็บไว้ตาม foundation pattern)
